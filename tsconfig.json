{
  "//": [
    "Type checking only. The extension has no build step and ships no compiled output:",
    "noEmit is not an optimisation here, it is the whole point.",
    "",
    "checkJs is deliberately FALSE. Files opt in one at a time with a `// @ts-check`",
    "pragma on line 1, so this config can land while D07 is still in flight without",
    "turning every in-progress module red.",
    "",
    "Only the small privacy modules currently opt into type checking."
  ],
  "compilerOptions": {
    "noEmit": true,
    "allowJs": true,
    "checkJs": false,
    "strict": true,
    "target": "ES2022",
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["chrome"],
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "noUncheckedIndexedAccess": true
  },
  "include": [
    "privacy/**/*.mjs"
  ],
  "exclude": [
    "node_modules",
    "vendor",
    "output",
    "xlsx.full.min.js",
    "mammoth.browser.min.js"
  ]
}
